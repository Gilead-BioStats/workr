name: Nightly Manifests

on:
  schedule:
    # Run nightly at 2am UTC
    - cron: '0 2 * * *'
  workflow_dispatch:

jobs:
  demo-manifest:
    uses: ./.github/workflows/manifest.yaml
    with:
      branch: ss-demo
      pkg_branch: dev
