name: Nightly Snapshots

on:
  schedule:
    # Run nightly at 2am UTC
    - cron: '0 2 * * *'
  workflow_dispatch:

jobs:
  demo-snapshot:
    uses: ./.github/workflows/snapshot.yaml
    with:
      branch: ss-demo
      pkg_branch: dev
