tbl: dm_raw
label: Raw DM ingestion spec
thresholds:
  warning: 0.01
  error: 0.05
steps:
  - col_exists:
      columns: [STUDYID, USUBJID]
  - col_is_character:
      columns: [STUDYID, USUBJID]
  - col_vals_not_null:
      columns: [STUDYID, USUBJID]
  - rows_distinct:
      columns: [USUBJID]
