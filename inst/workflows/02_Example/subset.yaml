meta:
  ID: subset
  Type: Example
  col: param
  value: cholesterol
steps:
  - name: workr::RunQuery
    output: df
    params:
      df: raw
      strQuery: "SELECT * FROM df WHERE param = 'cholesterol'"
