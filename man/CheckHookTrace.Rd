% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/CheckHookTrace.R
\name{CheckHookTrace}
\alias{CheckHookTrace}
\title{Check a hook trace}
\usage{
CheckHookTrace(dfTrace, lConfig = NULL, dfFailures = NULL)
}
\arguments{
\item{dfTrace}{\code{data.frame} A trace from \code{\link[=HookTrace]{HookTrace()}}.}

\item{lConfig}{\code{list} The \code{lConfig} the run used, so the checks know which
hooks were supposed to fire. Default \code{NULL}, i.e. check only what the
trace itself can prove.}

\item{dfFailures}{\code{data.frame} The \code{failures} element of a
\code{bContinueOnError = TRUE} run summary. Default \code{NULL}.}
}
\value{
A \code{data.frame} with columns \code{check}, \code{ok} and \code{detail}.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

Turns a \code{\link[=HookTrace]{HookTrace()}} into a small table of pass/fail assertions: did every
configured hook actually fire, did any hook raise, did any workflow ask for
a domain nothing could resolve, and did every workflow that ran persist
anything.

The point is that "the run was green" and "storage did the work" are
different claims. A run whose save hook silently never fired still produces
results in memory and exits 0; this is what catches that in an unattended
AWS or cron run.
}
\examples{
CheckHookTrace(HookTrace())

}
