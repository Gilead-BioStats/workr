% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/FilterWorkflows.R
\name{FilterWorkflows}
\alias{FilterWorkflows}
\title{Filter a workflow list by metadata fields}
\usage{
FilterWorkflows(lWorkflows, ...)
}
\arguments{
\item{lWorkflows}{\code{list} A named list of workflows, as returned by
\code{\link[=MakeWorkflowList]{MakeWorkflowList()}}.}

\item{...}{\code{name = value} pairs where each name is a \code{meta} field to filter
on (e.g., \code{Category}, \code{Type}, \code{Active}) and the value determines which
workflows are kept. Field lookup is case-insensitive. Workflows that lack
the specified field entirely are removed. The class of each value controls
matching behaviour:
\itemize{
\item \code{character}: keep workflows where any element of \code{meta[[field]]} matches
any element of \code{value} (case-insensitive).
\item \code{logical}: keep workflows where \code{meta[[field]]} coerces to the given
\code{TRUE}/\code{FALSE} value (coercion handles YAML string forms like \code{"true"}).
\item \code{numeric}: keep workflows where \code{meta[[field]]} equals any element of
\code{value} (equality only; no comparison operators).
\item other types: an informative error is raised.
}}
}
\value{
\code{list} A filtered named list of workflows.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#stable}{\figure{lifecycle-stable.svg}{options: alt='[Stable]'}}}{\strong{[Stable]}}

Filter a named list of workflows by matching zero or more \code{meta} fields
against supplied values. Each \code{name = value} pair in \code{...} is applied
sequentially (AND logic): a workflow must satisfy every filter to be
retained.
}
\examples{
lWorkflows <- MakeWorkflowList(strPath = "example_workflows", strPackage = "workr")

# Filter by a character field
lWorkflowsAnalysis <- FilterWorkflows(lWorkflows, Type = "Analysis")

# Multiple filters (AND logic)
lWorkflowsAnalysisSite <- FilterWorkflows(lWorkflows, Type = "Analysis", GroupLevel = "site")

}
