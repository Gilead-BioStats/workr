% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-hook-trace.R
\name{HookTrace}
\alias{HookTrace}
\alias{HookTraceReset}
\alias{HookTraceRecord}
\title{Hook trace}
\usage{
HookTraceReset()

HookTrace()

HookTraceRecord(
  key = NA_character_,
  domain = NA_character_,
  source = NA_character_,
  message = NA_character_
)
}
\arguments{
\item{key}{\code{string} Object key, path or table the provider acted on.}

\item{domain}{\code{string} Data domain the object satisfied.}

\item{source}{\code{string} Where the object resolved from, e.g. \code{"s3"},
\code{"lData"}, \code{"missing"}.}

\item{message}{\code{string} Free text.}
}
\value{
\code{HookTrace()} returns a \code{data.frame} with one row per event, in order.
\code{HookTraceReset()} invisibly returns \code{TRUE}.
\code{HookTraceRecord()} invisibly returns \code{TRUE}.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

Records every load/save hook invocation of a run: which scope fired, for
which workflow, through which provider, how long it took, and whether it
failed. When storage is a hook, this is the answer to "where did it go
wrong?" for a run nobody watched -- a cron job, an AWS batch run, an
overnight \verb{rv run}.

Providers add their own detail rows with \code{\link[=HookTraceRecord]{HookTraceRecord()}}: the object key
they read or wrote, the domain it satisfied, and where it came from. Those
rows inherit the scope, action and workflow of the hook that is running, so
a provider never has to know how it was invoked.
}
\details{
Every row has the same columns:
\itemize{
\item \code{scope} -- \code{project}, \code{phase} or \code{workflow}, i.e. which \code{lConfig} slot the
hook came from.
\item \code{action} -- \code{load} or \code{save}.
\item \code{workflow} -- \code{Type_ID} of the workflow, or \code{NA} for project-scope hooks.
\item \code{provider} -- the registered provider name, or \verb{<function>} for a literal
function.
\item \code{event} -- \code{hook} for the invocation summary, \code{detail} for a provider row.
\item \code{status} -- \code{ok} or \code{error} on \code{hook} rows.
\item \code{duration_ms} -- wall time of the invocation, on \code{hook} rows.
\item \code{key}, \code{domain}, \code{source} -- provider detail: the object acted on, the
domain it satisfied, and where it resolved from (e.g. \code{s3}, \code{lData},
\code{missing}).
\item \code{message} -- error message, or free text from a provider.
}

The trace lives for the session. \code{\link[=RunStudy]{RunStudy()}} resets it per run; call
\code{\link[=HookTraceReset]{HookTraceReset()}} yourself when driving \code{RunProject()} directly.
}
\examples{
HookTraceReset()
HookTrace()

}
