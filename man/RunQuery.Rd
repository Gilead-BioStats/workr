% Generated by hand
\name{RunQuery}
\alias{RunQuery}
\title{Run a SQL query on a data frame or DuckDB table}
\usage{RunQuery(strQuery, df, bUseSchema = FALSE, lColumnMapping = NULL)}
\description{Executes a SQL query on a data frame or DuckDB lazy table, creating
an in-memory DuckDB table when needed. The query must include a \code{FROM df}
placeholder.}
\arguments{
  \item{strQuery}{\code{character} SQL query to run, containing \code{"FROM df"}.}
  \item{df}{\code{data.frame} or \code{tbl_dbi} for the SQL query.}
  \item{bUseSchema}{\code{boolean} enforce data types with a schema.}
  \item{lColumnMapping}{\code{list} column specifications when \code{bUseSchema = TRUE}.}
}
\value{\code{data.frame} with query results.}
\examples{
if (requireNamespace("DBI", quietly = TRUE) &&
    requireNamespace("dbplyr", quietly = TRUE) &&
    requireNamespace("duckdb", quietly = TRUE)) {
  df <- data.frame(
    Name = c("John", "Jane", "Bob"),
    Age = c(25, 30, 35),
    Salary = c(50000, 60000, 70000)
  )

  query <- "SELECT Name, Salary FROM df WHERE Age >= 30"
  RunQuery(query, df)
}
}
