% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RunStudy.R
\name{RunStudy}
\alias{RunStudy}
\title{Run a project from a configuration file}
\usage{
RunStudy(
  strConfigPath = "study.yaml",
  strStage = "all",
  lParams = NULL,
  strOutputDir = NULL
)
}
\arguments{
\item{strConfigPath}{\code{character} Path to the YAML configuration file.
Default \code{"study.yaml"}.}

\item{strStage}{\code{character} Stage to run: a name from \code{project$stages}, or
\code{"all"} for every phase in \code{project$phases}. Default \code{"all"}.}

\item{lParams}{\code{list} Extra named values seeded into \code{lData} before the run,
e.g. a snapshot date resolved by the caller. Default \code{NULL}.}

\item{strOutputDir}{\code{character} Override the evidence directory. Default
\code{NULL}, i.e. \code{output$dir} from the configuration (with a \verb{stages/<stage>}
subdirectory for a single-stage run).}
}
\value{
Invisibly, a list with \code{stage}, \code{phases}, \code{summary} (the
\code{\link[=RunProject]{RunProject()}} result), \code{trace} and \code{checks}.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

\code{RunStudy()} runs a \code{\link[=RunProject]{RunProject()}} project described entirely by a YAML
configuration file: which phases exist, which subset of them a given stage
runs, which load/save hooks are attached at which scope, and where the run's
evidence is written.

It exists so a study repository holds configuration rather than scripts. A
cron job, an AWS batch job or an \verb{rv run} invocation needs one line:

\if{html}{\out{<div class="sourceCode">}}\preformatted{rv run -e 'workr::RunStudy(strStage = Sys.getenv("STAGE", "all"))'
}\if{html}{\out{</div>}}

Any string in the configuration may interpolate the environment as
\verb{$\{VAR\}} or \verb{$\{VAR:-default\}}, so a schedule varies a run by its
environment rather than by editing the study.
}
\section{Configuration}{
\if{html}{\out{<div class="sourceCode yaml">}}\preformatted{project:
  path: workflow              # catalog: directory of phase folders
  run_path: data/workflows-run # staged copy that actually runs
  include:                    # optional: the catalog subset to stage
    1_mappings: [AE.yaml]
  overlay: workflow-patches   # optional: study-owned workflow overrides
  phases: [1_mappings, 2_metrics, 3_reporting]
  stages:                     # optional named phase subsets
    mapping: [1_mappings]
    analysis: [2_metrics, 3_reporting]
  continue_on_error: true     # default TRUE: record failures, keep going
  expected_failures: []       # workflows allowed to fail
packages:
  require: [gsm.template]     # loaded so their providers self-register
  attach: [gsm.core]          # attached for unqualified step names
hooks:
  LoadData: gsm.template.load           # registered provider name
  SaveData: gsm.template.save
  phases:                     # optional: override a hook for one phase
    4_modules:
      SaveData: my.report.save
  project:
    LoadData: gsm.template::ProjectLoad # project scope takes pkg::fn
    SaveData: gsm.template::ProjectSave
config:                       # merged into lConfig for providers to read
  builder: gsm.template::UpdateConfig   # optional: build it from a package
  args:
    strStudyID: AA-AA-000-0000
    strSnapshotDate: $\{SNAPSHOT_DATE:-2025-06-30\}
  S3:
    bucket: data
run:
  before: [gsm.template::PrepareStorage]  # called before the project runs
output:
  dir: data/output            # evidence lands here, per stage
}\if{html}{\out{</div>}}

\code{hooks$LoadData} and \code{hooks$SaveData} -- and any \code{hooks$phases} entry -- name
providers registered with \code{\link[=register_load_provider]{register_load_provider()}} /
\code{\link[=register_save_provider]{register_save_provider()}}. \code{hooks$project} entries are functions, which
\code{RunStudy()} resolves from \code{"package::function"} strings, because
\code{\link[=RunProject]{RunProject()}} validates project-scope hooks by formals rather than by name.
}

\section{Evidence}{
Whatever the outcome, the stage's output directory gets:
\itemize{
\item \code{hook-trace.csv} -- every hook invocation (\code{\link[=HookTrace]{HookTrace()}}).
\item \code{hook-checks.csv} -- the generic assertions in \code{\link[=CheckHookTrace]{CheckHookTrace()}}.
\item \code{run-status.yaml} -- status, stage, phases, failures, check results.
\item \code{failure-context.yaml} -- error and traceback, when the run stops.
}

That is the difference between "the 2am job failed" and "the save hook for
\code{Mapped_AE} raised \code{NoSuchBucket} after loading 6 domains".
}

\examples{
\dontrun{
RunStudy("study.yaml")
RunStudy("study.yaml", strStage = "analysis")
}

}
