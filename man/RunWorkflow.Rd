% Generated by hand
\name{RunWorkflow}
\alias{RunWorkflow}
\title{Run a workflow via its specification}
\usage{RunWorkflow(lWorkflow, lData = NULL, lConfig = NULL, bReturnResult = TRUE, bKeepInputData = TRUE)}
\description{Runs a single workflow definition by invoking \code{RunStep} for each step and
capturing results.}
\arguments{
  \item{lWorkflow}{\code{list} metadata defining how the workflow should be run.}
  \item{lData}{\code{list} domain-level data.}
  \item{lConfig}{\code{list} configuration object with \code{LoadData} and \code{SaveData} functions.}
  \item{bReturnResult}{\code{boolean} return only last step result.}
  \item{bKeepInputData}{\code{boolean} keep input data when returning full workflow.}
}
\value{Results from the final step or the full workflow object.}
\examples{
sum_step <- function(lData, y) lData$value + y
lWorkflow <- list(
  meta = list(Type = "demo", ID = "001"),
  steps = list(
    list(name = "sum_step", output = "result", params = list(lData = "lData", y = "value"))
  )
)
lData <- list(value = 2)
RunWorkflow(lWorkflow, lData)
}
