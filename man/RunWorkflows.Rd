% Generated by hand
\name{RunWorkflows}
\alias{RunWorkflows}
\title{Run multiple workflows}
\usage{RunWorkflows(lWorkflows, lData = NULL, lConfig = NULL, bKeepInputData = FALSE, bReturnResult = TRUE, strResultNames = c("Type", "ID"))}
\description{Runs multiple workflows in sequence and returns a named list of results.}
\arguments{
  \item{lWorkflows}{\code{list} workflows to run.}
  \item{lData}{\code{list} domain-level data.}
  \item{lConfig}{\code{list} configuration object with \code{LoadData} and \code{SaveData} functions.}
  \item{bKeepInputData}{\code{boolean} keep input data when returning full workflow.}
  \item{bReturnResult}{\code{boolean} return only last step result.}
  \item{strResultNames}{\code{character} vector of length two specifying meta fields for result names.}
}
\value{A named list of results from \code{RunWorkflow}.}
\examples{
sum_step <- function(lData, y) lData$value + y
lWorkflows <- list(
  list(
    meta = list(Type = "demo", ID = "001"),
    steps = list(
      list(name = "sum_step", output = "result", params = list(lData = "lData", y = "value"))
    )
  )
)
lData <- list(value = 2)
RunWorkflows(lWorkflows, lData)
}
