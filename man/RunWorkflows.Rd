% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RunWorkflows.R
\name{RunWorkflows}
\alias{RunWorkflows}
\title{Convenience function to run multiple workflows}
\usage{
RunWorkflows(
  lWorkflows,
  lData = NULL,
  lConfig = NULL,
  bKeepInputData = FALSE,
  bReturnResult = TRUE,
  strResultNames = c("Type", "ID"),
  bContinueOnError = FALSE
)
}
\arguments{
\item{lWorkflows}{\code{list} A named list of metadata defining how the workflow should be run.}

\item{lData}{\code{list} A named list of domain-level data frames.}

\item{lConfig}{\code{list} Optional configuration object passed through to
\code{RunWorkflow()}. Supported hook fields:
\itemize{
\item \code{LoadData}: Optional function or registered provider name used before
spec validation. Functions must accept \code{lWorkflow}, \code{lConfig}, and
\code{lData} as named formals. \code{{workr}} includes a built-in \code{"gsm.datasim"}
provider that reads adapter settings from \code{lConfig$gsm.datasim}.
\item \code{SaveData}: Optional function or registered provider name used before
returning results when \code{bReturnResult = TRUE}. Functions must accept
\code{lWorkflow} and \code{lConfig} as named formals.
}}

\item{bKeepInputData}{\code{boolean} should the input data be included in \code{lData} after the workflow is run? Only
relevant when bReturnResult is FALSE. Default is \code{TRUE}.}

\item{bReturnResult}{\code{boolean} should \emph{only} the result from the last step (\code{lResults}) be returned? If false,
the full workflow (including \code{lResults}) is returned. Default is \code{TRUE}.}

\item{strResultNames}{\code{string} vector of length two, which describes the meta fields used to name the output.}

\item{bContinueOnError}{\code{logical} If \code{TRUE}, failed workflows are recorded
and later workflows still run. Default \code{FALSE} retains the existing
fail-fast behavior.}
}
\value{
A named list of results from \code{RunWorkflow()}, where the names
correspond to the workflow ID. When \code{bContinueOnError = TRUE}, returns a
summary with \code{results}, \code{status}, and \code{failures}.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#stable}{\figure{lifecycle-stable.svg}{options: alt='[Stable]'}}}{\strong{[Stable]}}

This function takes a list of workflows and a list of data as input. It runs each workflow and returns the
results as a named list where the names of the list correspond to the workflow ID (\code{meta$ID}).

Workflows are run in the order they are provided in \code{lWorkflows}. The results from each workflow are passed
as inputs (along with \code{lData}) for later workflows.
}
\examples{
sum_step <- function(x, y) {x + y}
lWorkflows <- list(
  list(
    meta = list(Type = "demo", ID = "001"),
    steps = list(
      list(name = "sum_step", output = "result", params = list(x = "value1", y = "value2"))
    )
  )
)
lData <- list(value1 = 1, value2 = 2)
RunWorkflows(lWorkflows, lData)

}
