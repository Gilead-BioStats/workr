% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/StageProject.R
\name{StageProject}
\alias{StageProject}
\title{Stage a project directory}
\usage{
StageProject(
  strPath,
  lInclude = NULL,
  strRunPath = file.path("data", "workflows-run"),
  strOverlayPath = NULL
)
}
\arguments{
\item{strPath}{\code{character} Path to the workflow catalog: a directory of
phase folders.}

\item{lInclude}{\code{list} Named by phase, each a character vector of workflow
file names to stage, e.g. \verb{list(}1_mappings\verb{ = c("AE.yaml"))}. \code{NULL}
stages every workflow in every phase found under \code{strPath}.}

\item{strRunPath}{\code{character} Directory to stage into. Replaced on each
call, so a stale workflow from a previous run cannot execute.}

\item{strOverlayPath}{\code{character} Optional directory mirroring the catalog's
phase structure. A file present there replaces the catalog's copy of the
same name. Default \code{NULL}.}
}
\value{
Invisibly, a named list of the file names staged per phase.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

Copies a subset of a workflow catalog into a run directory, optionally
applying study-owned overrides on the way.

A study that consumes a shared workflow library usually runs a named subset
of it, and sometimes needs a local fix to one workflow while the library
catches up. Doing that by hand means a staging script in every study;
\code{StageProject()} makes it two configuration keys, and leaves the catalog
itself untouched so what ran is always the catalog plus a declared,
reviewable list of overrides.
}
\examples{
\dontrun{
StageProject(
  strPath = "workflow",
  lInclude = list(`1_mappings` = c("AE.yaml", "SUBJ.yaml")),
  strRunPath = "data/workflows-run",
  strOverlayPath = "workflow-patches"
)
}

}
