% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RunStudy.R
\name{StudyConfig}
\alias{StudyConfig}
\title{Resolve a study configuration}
\usage{
StudyConfig(strConfigPath = "study.yaml", bLoadPackages = TRUE)
}
\arguments{
\item{strConfigPath}{\code{character} Path to the YAML configuration file.
Default \code{"study.yaml"}.}

\item{bLoadPackages}{\code{logical} Load and attach the packages the
configuration names, so a builder or provider it references is available.
Default \code{TRUE}.}
}
\value{
\code{list} The resolved configuration.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

Returns the \code{lConfig} a \code{\link[=RunStudy]{RunStudy()}} run would use: the configuration file's
\code{config} block with its environment references expanded, built by
\code{config$builder} when one is named, and with the \code{project} block attached as
\code{Project}.

Use it wherever a tool needs the same view of a study the pipeline has --
pulling its workflow library, moving its stored state between environments --
so those entry points stay wiring rather than a second implementation of the
configuration.
}
\examples{
\dontrun{
lConfig <- StudyConfig()
lConfig$StudyID
}

}
