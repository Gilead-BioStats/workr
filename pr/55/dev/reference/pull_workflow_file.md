# Pull a single workflow file from GitHub

Pull a single workflow file from GitHub

## Usage

``` r
pull_workflow_file(
  full_repo,
  sha,
  api_path,
  local_path,
  source_repo = full_repo,
  seen_files = NULL
)
```
