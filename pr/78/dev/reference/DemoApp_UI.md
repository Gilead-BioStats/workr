# Demo app UI

**\[stable\]** Builds the Shiny user interface for the demo application.

## Usage

``` r
DemoApp_UI(lWorkflows)
```

## Arguments

- lWorkflows:

  `list` Named list of workflows.

## Value

A Shiny UI definition.
