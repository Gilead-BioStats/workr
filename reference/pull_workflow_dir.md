# Recursively pull a directory of workflow files

Recursively pull a directory of workflow files

## Usage

``` r
pull_workflow_dir(
  full_repo,
  sha,
  api_path,
  local_dir,
  source_repo = full_repo,
  seen_files = NULL
)
```
